# R5 - 回傳全收確認

## 接口資訊

| 項目 | 內容 |
|------|------|
| **接口名稱** | 回傳全收確認 |
| **接口代號** | R5 |
| **URL** | POST `/wmService/v1/PO/PoCfmReceipt` |
| **通訊方式** | POST |
| **請求格式** | JSON |
| **回應格式** | JSON |
| **提供方** | WMS |
| **調用方** | 外部系統 (Android App) |

## 功能說明

輸入板數與儲位，確認過帳並產生板標籤。此接口會檢查所有 PO 項次是否皆有件數，確認後將採購單狀態變更為「收貨完成」。

## 請求參數

| 項次 | 參數名稱 | 參數說明 | 傳輸型態 | DB資料類型 | 字串最大長度 | 備註 | 必填 |
|------|---------|--------|--------|-----------|-----------|------|------|
| 1 | requestId | 請求編號 | String(36) | GUID | 36 | 每個請求唯一，重複提交使用同一編號 | V |
| 2 | wmsAsnNumber | WMS預期收貨單號 | String(10) | Char | 10 | 帶 R1 回傳的 "WMS預期收貨單號" | V |
| 3 | storerCode | 貨主代碼 | String(15) | Char | 15 | 帶 R1 回傳的 "貨主代碼" | V |
| 4 | externReceiptNumber | 外部收貨編號 | String(30) | Char | 30 | 外部收貨編號＝採購單號 | V |
| 5 | recPalletQty | 總板數 | String(10) | int | - | 用戶端輸入的板數，整數 | - |
| 6 | recLocCode | 儲位編號 | String(10) | Char | 10 | 用戶端選擇的儲位編號，帶 R4 的 "儲位編號" | V |

### 請求範例

```json
{
  "requestId": "fb1a6bb8-21cd-490e-9f47-962cf99ec089",
  "wmsAsnNumber": "0000000001",
  "storerCode": "69512619",
  "externReceiptNumber": "API21052600601",
  "recPalletQty": "5",
  "recLocCode": "A01"
}
```

### JSON Schema

```typescript
interface R5Request {
  requestId: string;        // 格式: UUID v4
  wmsAsnNumber: string;    // 來自 R1 回傳
  storerCode: string;      // 來自 R1 回傳
  externReceiptNumber: string; // 來自 R1 回傳
  recPalletQty: string;    // 總板數（整數字串）
  recLocCode: string;      // 來自 R4 回傳的儲位編號
}
```

## 回應參數

| 項次 | 參數名稱 | 參數說明 | 傳輸型態 | DB資料類型 | 字串最大長度 | 備註 | 必填 |
|------|---------|--------|--------|-----------|-----------|------|------|
| 1 | requestId | 請求編號 | String(36) | Char | 36 | 傳回原始請求編號 | V |
| 2 | status | API回應結果代碼 | String(1) | Char | - | 成功代碼：S、失敗代碼：F | V |
| 3 | message | API回應結果訊息 | String(255) | Char | 255 | - | V |
| 4 | data | 回覆資料內容 | Object | - | - | 成功時有值 | - |
| 5 | warehouseCode | 倉庫 | String(10) | Char | 10 | 實體倉庫代碼＝回廠工廠 | V |
| 6 | wmsAsnNumber | WMS預期收貨單號 | String(10) | Char | 10 | WMS單號主鍵值 | V |
| 7 | storerCode | 貨主代碼 | String(15) | Char | 15 | 貨主主檔代碼 | V |
| 8 | externReceiptNumber | 外部收貨編號 | String(30) | Char | 30 | 外部收貨編號＝採購單號 | V |
| 9 | vendorName | 供應商名稱 | String(30) | Char | 30 | - | V |
| 10 | asnFishingGroundName | 單據漁廠名稱 | String(100) | Char | 100 | 明細漁廠名稱串連（資料不重覆） | - |
| 11 | recPalletQty | 總板數 | String(10) | int | - | 總板數，整數 | V |
| 12 | asnTotalQty | 單據總數量 | String(20) | decimal(19,5) | - | 單據總數量＝PO需求總數量，小數 | - |
| 13 | asnTotalUom | 單據總數量單位 | String(18) | NVarChar | 18 | WMS庫存單位 | - |
| 14 | recTotalPackQty | 已收總件數 | String(10) | int | - | 已收總件數，整數 | - |
| 15 | palletLabelList | 板標籤清單資料列 | Array | - | - | 板標籤資料，成功時有值 | - |
| 16 | lblExternReceiptNumber | 標籤採購單號資料 | String(40) | Char | 40 | 標籤採購單號資料，例如：採購單號 API21052600601 | V |
| 17 | lblVendorName | 標籤供應商名稱 | String(30) | Char | 30 | 供應商名稱 | V |
| 18 | lblPalletQty | 標籤板數 | String(10) | int | - | 板數 | V |
| 19 | lblLocCode | 標籤儲位編號 | String(10) | Char | 10 | 儲位編號 | V |
| 20 | lblSku | 標籤貨號 | String(50) | Char | 50 | 貨號 | V |
| 21 | lblItemName | 標籤品名 | String(200) | NVarChar | 200 | 品名 | V |

### JSON Schema (回應)

```typescript
interface R5Response {
  requestId: string;
  status: "S" | "F";
  message: string;
  data: {
    warehouseCode: string;
    wmsAsnNumber: string;
    storerCode: string;
    externReceiptNumber: string;
    vendorName: string;
    asnFishingGroundName: string;  // 明細漁廠名稱串連
    recPalletQty: string;          // 總板數
    asnTotalQty: string;           // 單據總數量
    asnTotalUom: string;           // 單據總數量單位
    recTotalPackQty: string;       // 已收總件數
    palletLabelList: Array<{
      lblExternReceiptNumber: string;  // 標籤採購單號
      lblVendorName: string;           // 標籤供應商名稱
      lblPalletQty: string;            // 標籤板數
      lblLocCode: string;              // 標籤儲位編號
      lblSku: string;                  // 標籤貨號
      lblItemName: string;             // 標籤品名
    }>;
  } | null;
  errors: Array<{code: string; message: string}> | null;
}
```

### 回應範例 (成功)

```json
{
  "requestId": "fb1a6bb8-21cd-490e-9f47-962cf99ec089",
  "status": "S",
  "message": "收貨確認成功",
  "data": {
    "warehouseCode": "1020",
    "wmsAsnNumber": "0000000001",
    "storerCode": "69512619",
    "externReceiptNumber": "API21052600601",
    "vendorName": "供應商名稱",
    "asnFishingGroundName": "漁廠A、漁廠B",
    "recPalletQty": "5",
    "asnTotalQty": "500.000",
    "asnTotalUom": "KG",
    "recTotalPackQty": "300",
    "palletLabelList": [
      {
        "lblExternReceiptNumber": "採購單號 API21052600601",
        "lblVendorName": "供應商名稱",
        "lblPalletQty": "5",
        "lblLocCode": "A01",
        "lblSku": "SKU001",
        "lblItemName": "鮭魚切片"
      }
    ]
  },
  "errors": null
}
```

### 回應範例 (失敗 - 項次未收完)

```json
{
  "requestId": "fb1a6bb8-21cd-490e-9f47-962cf99ec089",
  "status": "F",
  "message": "失敗-資料的邏輯錯誤",
  "data": null,
  "errors": [
    {
      "code": "F983",
      "message": "項次00010未有件數"
    }
  ]
}
```

### 回應範例 (失敗 - 採購單已過帳)

```json
{
  "requestId": "fb1a6bb8-21cd-490e-9f47-962cf99ec089",
  "status": "F",
  "message": "失敗-資料的邏輯錯誤",
  "data": null,
  "errors": [
    {
      "code": "F983",
      "message": "採購單已過帳，無法再次確認"
    }
  ]
}
```

## 使用方式

### Curl 範例

```bash
curl -X POST http://192.168.x.x/wmService/v1/PO/PoCfmReceipt \
  -H "Authorization: Bearer {accessToken}" \
  -H "Content-Type: application/json" \
  -d '{
    "requestId": "fb1a6bb8-21cd-490e-9f47-962cf99ec089",
    "wmsAsnNumber": "0000000001",
    "storerCode": "69512619",
    "externReceiptNumber": "API21052600601",
    "recPalletQty": "5",
    "recLocCode": "A01"
  }'
```

### Android Kotlin 範例

```kotlin
data class ConfirmReceiptResult(
    val warehouseCode: String,
    val wmsAsnNumber: String,
    val storerCode: String,
    val externReceiptNumber: String,
    val vendorName: String,
    val asnFishingGroundName: String,
    val recPalletQty: Int,
    val asnTotalQty: String,       // 單據總數量
    val asnTotalUom: String,       // 單據總數量單位
    val recTotalPackQty: Int,      // 已收總件數
    val palletLabelList: List<PalletLabel>
)

data class PalletLabel(
    val lblExternReceiptNumber: String,
    val lblVendorName: String,
    val lblPalletQty: Int,
    val lblLocCode: String,
    val lblSku: String,
    val lblItemName: String
)

class ReceivingRepository {
    private val baseUrl = "http://192.168.x.x/wmService/v1"
    private val client = OkHttpClient()

    suspend fun confirmReceipt(
        wmsAsnNumber: String,
        storerCode: String,
        externReceiptNumber: String,
        recPalletQty: Int,
        recLocCode: String
    ): Result<ConfirmReceiptResult> = withContext(Dispatchers.IO) {
        val requestId = UUID.randomUUID().toString()
        val token = getAccessToken()

        try {
            val jsonRequest = JSONObject().apply {
                put("requestId", requestId)
                put("wmsAsnNumber", wmsAsnNumber)
                put("storerCode", storerCode)
                put("externReceiptNumber", externReceiptNumber)
                put("recPalletQty", recPalletQty.toString())
                put("recLocCode", recLocCode)
            }

            val body = jsonRequest.toString().toRequestBody("application/json".toMediaType())
            val request = Request.Builder()
                .url("$baseUrl/PO/PoCfmReceipt")
                .addHeader("Authorization", "Bearer $token")
                .post(body)
                .build()

            val response = client.newCall(request).execute()
            val responseBody = response.body?.string() ?: throw Exception("無回應")

            val jsonResponse = JSONObject(responseBody)

            if (jsonResponse.getString("status") == "S") {
                val data = jsonResponse.getJSONObject("data")

                // 解析板標籤清單
                val labelList = data.getJSONArray("palletLabelList")
                val labels = mutableListOf<PalletLabel>()
                for (i in 0 until labelList.length()) {
                    val label = labelList.getJSONObject(i)
                    labels.add(
                        PalletLabel(
                            lblExternReceiptNumber = label.getString("lblExternReceiptNumber"),
                            lblVendorName = label.getString("lblVendorName"),
                            lblPalletQty = label.getString("lblPalletQty").toInt(),
                            lblLocCode = label.getString("lblLocCode"),
                            lblSku = label.getString("lblSku"),
                            lblItemName = label.getString("lblItemName")
                        )
                    )
                }

                val result = ConfirmReceiptResult(
                    warehouseCode = data.getString("warehouseCode"),
                    wmsAsnNumber = data.getString("wmsAsnNumber"),
                    storerCode = data.getString("storerCode"),
                    externReceiptNumber = data.getString("externReceiptNumber"),
                    vendorName = data.getString("vendorName"),
                    asnFishingGroundName = data.getString("asnFishingGroundName"),
                    recPalletQty = data.getString("recPalletQty").toInt(),
                    asnTotalQty = data.getString("asnTotalQty"),
                    asnTotalUom = data.getString("asnTotalUom"),
                    recTotalPackQty = data.getString("recTotalPackQty").toInt(),
                    palletLabelList = labels
                )

                Result.success(result)
            } else {
                val message = jsonResponse.getString("message")
                Result.failure(Exception(message))
            }
        } catch (e: Exception) {
            Result.failure(e)
        }
    }
}
```

## 關聯接口

此接口為進貨作業的最後一個接口：

| 上一個接口 | 說明 |
|-----------|------|
| [R4 - 取得收貨核對明細](R4-取得收貨核對明細.md) | 提供 `recLocCode` 作為 R5 輸入 |

## 檢核規則

1. **完整性檢查**:
   - 檢查所有 PO 項次是否皆有件數
   - 若有任何項次件數為 0，回傳 F983 錯誤，訊息包含未有件數的項次代碼

2. **狀態檢查**:
   - 檢查採購單是否已過帳
   - 若已過帳，回傳 F983 錯誤

3. **板數驗證**:
   - `recPalletQty` 必須為整數
   - 若非整數，回傳 F981 錯誤

## 錯誤代碼

| 錯誤代碼 | HTTP | 說明 | 因應方式 |
|---------|------|------|--------|
| F119 | 401 | Token 驗證失效 | 重新登入取得 Token |
| F981 | 400 | 資料格式錯誤 | 檢查 `recPalletQty` 是否為整數 |
| F983 | 400 | 業務邏輯錯誤 | 1. 檢查所有項次是否皆有件數<br>2. 檢查採購單是否已過帳 |
| F999 | 500 | 伺服器異常 | 聯繫技術支援 |

## 注意事項

1. **板標籤處理**:
   - 成功回應會包含 `palletLabelList`，用戶應列印標籤
   - 標籤內容包含採購單號、供應商、板數、儲位、貨號、品名
   - 可多個板合併列印一張標籤

2. **完整性檢查**:
   - R5 執行前，建議用戶端先自行檢查所有項次是否皆有件數
   - 避免呼叫 R5 後才收到錯誤

3. **板數輸入**:
   - `recPalletQty` 為用戶輸入的板數總量
   - 通常為本次收貨所使用的板數

4. **儲位選擇**:
   - `recLocCode` 必須來自 R4 的 `locList`
   - 用戶從 R4 的儲位選單中選擇

5. **狀態變更**:
   - R5 成功後，採購單狀態變更為「收貨完成」
   - 該採購單後續無法再次呼叫 R5
   - 若需修改，需聯繫後台人員

6. **重複提交防護**:
   - 同一採購單的第二個 R5 呼叫會收到 F983 錯誤
   - 用戶端應顯示「此採購單已完成收貨」訊息

7. **列印功能**:
   - 建議在 R5 成功後自動列印板標籤
   - 或提供「重新列印標籤」功能（需後端支援）

8. **異常處理**:
   - 若 R5 呼叫失敗，用戶端應提供「重新嘗試」選項
   - 但務必檢查是否為可重試錯誤（如網路逾時）
